zookeeper:
  hostname: {{ hostname }}
  image: {{ image }}
  name: {{ name }}
  ports:
  {% for port in ports %}
  - {{ port }}
  {% endfor %}
privileged: {{ privileged }}
  restart: {{ restart_policy }}
  volumes:
  {% for volume in volumes %}
  - {{ volume }}
  {% endfor %}